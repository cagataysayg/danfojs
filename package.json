{
    "name": "danfo",
    "version": "1.1.2",
    "private": true,
    "workspaces": [
        "danfojs-browser/**"
    ],
    "scripts": {
        "install": "cd src/danfojs-browser && yarn",
        "build": "cd src/danfojs-browser && yarn build:clean",
        "test": "cd src/danfojs-base && yarn && cd ../danfojs-browser && yarn && yarn test:clean"
    }
}
